spring:
  application:
    name: sca-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  # Spring Insight配置
  insight:
    enabled: true
    service-name: sca-user
    datasource:
      url: jdbc:mysql://localhost:3306/spring-insight?useSSL=false&serverTimezone=UTC
      username: root
      password: 123456

server:
  port: 8083