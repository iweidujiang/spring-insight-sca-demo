# 可选：复制为 .env 后由 docker compose 自动加载
#
# Maven 本地库（构建镜像时注入 Spring Insight 构件）
# LOCAL_M2_REPOSITORY=C:/Users/你的用户名/.m2/repository
#
# 与外部已启动的 Nacos 共用的 Docker 网络名（须与 docker run --network 一致）
# DOCKER_NETWORK=my-network

LOCAL_M2_REPOSITORY=
DOCKER_NETWORK=my-network
