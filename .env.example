# 复制本文件为 .env 后按本机路径修改（.env 勿提交）。
#
# ---------- 使用 Spring Insight 前的准备 ----------
# 1) 在 Spring Insight 工程（独立仓库）执行：
#      mvn clean install -DskipTests
#    得到 agent-starter 进入本机 Maven 仓库，以及 insight-server 可执行 jar。
# 2) 填写下面两个路径后再：docker compose up -d --build
#
# Maven localRepository 目录（里面应有 io/github/iweidujiang/spring-insight-agent-starter/...）
# Windows 默认一般为 C:/Users/<你>/.m2/repository
# 若 ~/.m2/settings.xml 里改过 localRepository，填那个目录（例如 D:/Java/mvn_repo）
MAVEN_REPO=D:/Java/mvn_repo
#
# insight-server 可执行 fat jar（Spring Insight 构建产物）
# 示例：D:/github-project/spring-insight/insight-server/target/insight-server-0.1.0-SNAPSHOT.jar
INSIGHT_SERVER_JAR=D:/github-project/spring-insight/insight-server/target/insight-server-0.1.0-SNAPSHOT.jar
#
# Nacos 账号（compose 内 sca-nacos 与各业务服务共用）
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos
#
# 可选：构建用 Maven 镜像 / 运行 insight-server 的 JRE 镜像
# MAVEN_IMAGE=maven:3.9.9-eclipse-temurin-21
# JAVA_RUNTIME_IMAGE=eclipse-temurin:21-jre-alpine
#
# 可选：仅在你自定义 Nacos 鉴权参数时修改
# NACOS_AUTH_IDENTITY_KEY=serverIdentity
# NACOS_AUTH_IDENTITY_VALUE=security
# NACOS_AUTH_TOKEN=0123456789abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz
