# 复制为 .env（勿提交入库）。compose-up.ps1 会读取其中的 MAVEN_LOCAL_REPOSITORY。
#
# Maven 本地仓库根目录 = settings.xml 里 <localRepository>（目录下须有 io、org 等子目录）
# Windows 示例（按本机修改）:
# MAVEN_LOCAL_REPOSITORY=D:/java/mvn_repo
#
# DOCKER_NETWORK=my-network

MAVEN_LOCAL_REPOSITORY=
DOCKER_NETWORK=my-network
