# Docker：强制 WebFlux；Insight 使用 WebFlux 过滤器 + Starter 中的 insight-collector-service（未引入 MVC 版 insight-collector）
spring:
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: nacos-standalone:8848
        username: nacos
        password: nacos

server:
  address: 0.0.0.0
