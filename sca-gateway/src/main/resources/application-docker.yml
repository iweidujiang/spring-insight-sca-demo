# Docker：WebFlux 网关 + Agent 上报到 insight-server（无需 exclusion MVC）
spring:
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: sca-nacos:8848
        username: nacos
        password: nacos
  insight:
    server-url: http://insight-server:9966

server:
  address: 0.0.0.0
