# Docker：Gateway 仅 WebFlux，勿引入 Servlet MVC / Spring Insight（见 README）
spring:
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: nacos-standalone:8848
        username: nacos
        password: nacos

server:
  address: 0.0.0.0
