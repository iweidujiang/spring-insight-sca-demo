spring:
  cloud:
    nacos:
      discovery:
        # 与本机已启动的 nacos-standalone 同网（compose 使用 external my-network）
        server-addr: nacos-standalone:8848
        username: nacos
        password: nacOsdev316
  insight:
    enabled: true
    server-url: http://host.docker.internal:9966
    ingest-token: change-me
    sample-rate: 1.0
    http-tracing-enabled: true
    context-propagation-enabled: true
    micrometer-enabled: true
    diagnostic-logs: true
    jvm-metrics:
      enabled: true
    db-metrics:
      enabled: true

server:
  address: 0.0.0.0

demo:
  product-base-url: http://sca-product:18082

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
