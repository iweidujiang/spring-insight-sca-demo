spring:
  application:
    name: sca-order
  cloud:
    nacos:
      discovery:
        # 宿主机映射端口（与 nacos-standalone -p 38848:8848 一致）
        server-addr: 127.0.0.1:38848
        username: nacos
        password: nacOsdev316
  insight:
    # serviceName 默认取 spring.application.name；上报到独立监测中心
    server-url: http://localhost:9966

server:
  port: 18081
